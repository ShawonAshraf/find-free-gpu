[project]
name = "find-free-gpu"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=3.5.0",
    "ruff>=0.14.8",
]
